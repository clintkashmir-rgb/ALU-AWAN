rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read/write for all authenticated users (including anonymous)
      allow read, write: if request.auth != null;
    }
  }
}
